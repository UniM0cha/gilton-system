import React from 'react';
import { createRoot } from 'react-dom/client';

const App = () => <div>길튼 시스템 클라이언트 초기 설정</div>;

const root = createRoot(document.getElementById('root')!);
root.render(<App />);
